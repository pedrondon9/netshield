# Este workflow ha sido integrado en ci.yml como jobs encadenados.
# Se mantiene el archivo para no romper referencias externas,
# pero no tiene disparador automático.

name: CD — (desactivado, ver ci.yml)

on:
  workflow_dispatch:
    inputs:
      razon:
        description: "Motivo del deploy manual"
        required: true
